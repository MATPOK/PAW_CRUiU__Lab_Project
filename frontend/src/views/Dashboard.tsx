import { Typography, Container } from '@mui/material';

export default function Dashboard() {
  return (
    <Container>
      <Typography variant="h4" sx={{ color: 'white' }}>Pulpit Nawigacyjny</Typography>
      <Typography sx={{ color: 'gray' }}>Tu będą wykresy (wkrótce).</Typography>
    </Container>
  );
}